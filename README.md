# Build
```bash
python3 build.py
```

## Flags
- `--mode <release/debug>` - Select the build mode.
- `--size <mb>` - Select the write / read size for a test.
- `--iters <count>` - Select amount of files.
- `--out <path>` - Select the destination for the output file. 
